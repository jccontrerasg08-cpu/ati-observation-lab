[build]
builder = "RAILPACK"

[deploy]
startCommand = "uvicorn observation_lab.app:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/healthz"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 2
